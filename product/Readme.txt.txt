First u have go to use cmd get into this folder and compile the compiler with go.bat

then run: compiler.exe example[number].txt(e.g: compiler.exe example11.txt )

then if example has no error,there will be a informaion.xml, otherwise will be a errormessage.txt

for these above all this, make sure ur computer has installed bison/flex and MinGW
